Put your real images in this folder — your profile photo, project
screenshots, a favicon, etc. Anything you put here can be referenced
in your code as `/filename.ext` (e.g. `public/profile.jpg` → `/profile.jpg`).

See the main README.md at the project root for exactly which files
to add and which lines of code to update.
